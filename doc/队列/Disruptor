disruptor 为解决内存队列的延迟问题
解决队列速度慢的设计方式：
1.环形数组结构
为了避免垃圾回收，采用数组而非链表。同时，数组对处理器的缓存机制更友好
2.元素位置定位：
数组长度2^n，通过位运算，加快定位的速度
3.无锁设计
每个生产者或者消费者线程，会先申请可以操作的元素在数组中的位置，申请到之后，直接在该位置写入或者读取数据

无锁设计：
通过原子变量CAS,保证操作的线程安全
